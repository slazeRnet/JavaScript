function parse(){
    let initialNumber = 27;
    let doubleNumber = initialNumber / 4; // 6.75
    let parsedToInt = parseInt(initialNumber / 4); // 6
    console.log(doubleNumber);
    console.log(parsedToInt);

}

parse();
